doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Contact Us - GrooveGrid
    link(rel="stylesheet", href="contact-us.css")
    script(defer, src="contact-us.js")
  body
    header
      a.logo(href="/")
        img.logo-image(src="images/mylogogr.png", alt="GrooveGrid Logo")

    main
      section.contact-form
        h1 Contact Us
        p We're here to help! Fill out the form below, and we'll get back to you as soon as possible.

        form#contactForm
          label(for="name") Name:
          input#name(type="text", name="name", placeholder="Your Name", required=true)

          label(for="email") Email:
          input#email(type="email", name="email", placeholder="Your Email", required=true)

          label(for="subject") Subject:
          input#subject(type="text", name="subject", placeholder="Subject", required=true)

          label(for="message") Message:
          textarea#message(name="message", rows="5", placeholder="Your Message", required=true)

          button(type="submit") Send Message

    footer
      p © 2024 GrooveGrid. All Rights Reserved.

    //- Thank You Message Modal
    #thankYouModal.modal
      .modal-content
        span.close &times;
        h2 Thank You!
        p Your message has been sent successfully. We will get back to you soon.
